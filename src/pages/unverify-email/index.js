import { connect } from "react-redux";
import { default as UnverifyEmail } from "./Unverify-Email";

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(UnverifyEmail);
